extends layout

block content
  h1= title
  - let items = results;
   div.container-fluid 
    div.row
     each elephant,index in items 
      div.col-sm-2(onclick=`buy(${index})`) #{elephant.name}
      div.col-sm-2(onclick=`buy(${index})`) #{elephant.age}
      div.col-sm-2(onclick=`buy(${index})`) #{elephant.species}
      br
      hr

  script. 
       function buy(index)
       {
        alert(`you purchased Item: ${index+1}`)
       }


    

  